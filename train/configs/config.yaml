seed: 123
# num_gpus: 1
# num_epochs: 100
resume_dir: None
print_interval: 10

SOLVER:
  method: adam
  lr: 0.0001
  weight_decay: 0.00001
  ims_per_batch: 16

TEST:
  ims_per_batch: 16

DATASET:
  # dataset_path: /new_disk2/yuzh/PlaneNetData/
  # batch_size: 16
  num_workers: 16
  if_val_dist: True

MODEL_BRDF:
  load_pretrained_pth: False